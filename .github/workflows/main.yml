name: Second workflow
on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $SHELL
          pwd
      - working-directory: ./script/test/123
        run: pwd
   
