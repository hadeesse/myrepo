name: my cache
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu-latest, window-latest, macos-latest]
        v: [12, 13, 14]
    steps:
      - run: |
          echo "================="
          echo ${{ matrix.os }} : ${{ matrix.v }}
