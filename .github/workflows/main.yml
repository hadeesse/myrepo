name: my cache
on: push

# simple matrix

# jobs:
# build:
#   runs-on: ubuntu-latest
#   strategy:
#     matrix:
#       os: [ubuntu-latest, window-latest, macos-latest]
#       v: [12, 13, 14]
#   steps:
#     - run: |
#         echo ${{ matrix.os }} : ${{ matrix.v }}

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu-latest, window-latest]
        v: [12, 13]
        include:
          - os: ubuntu-latest
            key: value
    steps:
      - run: |
          echo ${{ matrix.os }} : ${{ matrix.v }} : ${{ matrix.key }}
